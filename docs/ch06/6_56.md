# 6.56 ＦＭ音源４オペレータモードの接続形態

[← 目次](../index.md)

４オペレータモードの音色設定における、  
`CON`（connection：オペレータの接続パターン）  
の内容を示します。

対象となる音色定義は、次の通りです。

```
#MB:FMS_4OP
#MB:FMS_OPM （ＯＰＭ互換）
#MB:FMS_OPNA（ＯＰＮＡ互換）
#MB:FMS_OPN （ＯＰＮ互換）
```

以下に、`CON`番号ごとの接続図を示します。

「out」は音声出力です。  
色が白のオペレータがモジュレータです。  
色がオレンジのオペレータがキャリアです。  
キャリアが複数ある`CON`では、コーラスや和音の効果を出すことも可能です。

全てのオペレータにセルフフィードバック機能があります。

４オペレータモードの `CON` は、0〜13 で指定します。  
（通常 0〜7、拡張 8〜13）

### 通常の接続形態（CON=0 〜 CON=7）：

```mermaid
flowchart LR
  subgraph CON["CON = 0"]
    direction LR
    OP1("OP1") --> OP2("OP2") --> OP3("OP3") --> OP4("OP4") --> OUT(("out"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fff, stroke:#000, color:#000
style OP3 fill:#fff, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 1"]
    direction LR
    OP1("OP1") --> S(("+")) --> OP3("OP3") --> OP4("OP4") --> OUT(("out"))
    OP2("OP2") --> S(("+"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fff, stroke:#000, color:#000
style OP3 fill:#fff, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style S   fill:#fff, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 2"]
    direction LR
    OP2("OP2") --> OP3("OP3") --> S(("+")) --> OP4("OP4") --> OUT(("out"))
    OP1("OP1") --> S(("+"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fff, stroke:#000, color:#000
style OP3 fill:#fff, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style S   fill:#fff, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 3"]
    direction LR
    OP1("OP1") --> OP2("OP2") --> S(("+")) --> OP4("OP4") --> OUT(("out"))
    OP3("OP3") --> S(("+"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fff, stroke:#000, color:#000
style OP3 fill:#fff, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style S   fill:#fff, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 4"]
    direction LR
    OP1("OP1") --> OP2("OP2") --> OUT(("out"))
    OP3("OP3") --> OP4("OP4") --> OUT(("out"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fb0, stroke:#000, color:#000
style OP3 fill:#fff, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 5"]
    direction LR
    OP1("OP1") --> OP2("OP2") --> OUT(("out"))
    OP1("OP1") --> OP3("OP3") --> OUT(("out"))
    OP1("OP1") --> OP4("OP4") --> OUT(("out"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fb0, stroke:#000, color:#000
style OP3 fill:#fb0, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 6"]
    direction LR
    OP1("OP1") --> OP2("OP2") --> OUT(("out"))
    OP3("OP3") --> OUT(("out"))
    OP4("OP4") --> OUT(("out"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fb0, stroke:#000, color:#000
style OP3 fill:#fb0, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 7"]
    direction LR
    OP1("OP1") --> OUT(("out"))
    OP2("OP2") --> OUT(("out"))
    OP3("OP3") --> OUT(("out"))
    OP4("OP4") --> OUT(("out"))
  end
style OP1 fill:#fb0, stroke:#000, color:#000
style OP2 fill:#fb0, stroke:#000, color:#000
style OP3 fill:#fb0, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

### 独自拡張の接続形態（CON=8 〜 CON=13）：

```mermaid
flowchart LR
  subgraph CON["CON = 8"]
    direction LR
    OP1("OP1") --> S(("+")) --> OP4("OP4") --> OUT(("out"))
    OP2("OP2") --> S(("+"))
    OP3("OP3") --> S(("+"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fff, stroke:#000, color:#000
style OP3 fill:#fff, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style S   fill:#fff, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 9"]
    direction LR
    OP1("OP1") --> OP2("OP2") --> OP3("OP3") --> OUT(("out"))
    OP2("OP2") --> OP4("OP4") --> OUT(("out"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fff, stroke:#000, color:#000
style OP3 fill:#fb0, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 10"]
    direction LR
    OP1("OP1") --> OP2("OP2") --> OP3("OP3") --> OUT(("out"))
    OP4("OP4") --> OUT(("out"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fff, stroke:#000, color:#000
style OP3 fill:#fb0, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 11"]
    direction LR
    OP1("OP1") --> S(("+")) --> OP3("OP3") --> OUT(("out"))
    OP2("OP2") --> S(("+")) --> OP4("OP4") --> OUT(("out"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fff, stroke:#000, color:#000
style OP3 fill:#fb0, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style S   fill:#fff, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 12"]
    direction LR
    OP1("OP1") --> S(("+")) --> OP3("OP3") --> OUT(("out"))
    OP2("OP2") --> S(("+"))
    OP4("OP4") --> OUT(("out"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fff, stroke:#000, color:#000
style OP3 fill:#fb0, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style S   fill:#fff, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

```mermaid
flowchart LR
  subgraph CON["CON = 13"]
    direction LR
    OP1("OP1") --> OP2("OP2") --> OUT(("out"))
    OP1("OP1") --> OP3("OP3") --> OUT(("out"))
    OP4("OP4") --> OUT(("out"))
  end
style OP1 fill:#fff, stroke:#000, color:#000
style OP2 fill:#fb0, stroke:#000, color:#000
style OP3 fill:#fb0, stroke:#000, color:#000
style OP4 fill:#fb0, stroke:#000, color:#000
style OUT fill:#ff0, stroke:#000, color:#000
linkStyle default stroke:#000
```

